{
  "name": "@helpcenter-rag/root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test": "npm run test --workspaces",
    "type-check": "cd frontend && npx vue-tsc --noEmit && cd ../backend && npx tsc --noEmit && cd ../content-processor && npx tsc --noEmit"
  },
  "devDependencies": {
    "@pinia/testing": "^0.1.7",
    "@vue/test-utils": "^2.4.8",
    "husky": "^9.1.7",
    "jsdom": "^29.0.2",
    "vitest": "^4.1.5"
  }
}
