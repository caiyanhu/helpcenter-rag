apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helpcenter-rag.fullname" . }}-secrets
  namespace: {{ include "helpcenter-rag.namespace" . }}
  labels:
    {{- include "helpcenter-rag.labels" . | nindent 4 }}
type: Opaque
stringData:
  deepseek-api-key: {{ .Values.backend.secrets.deepseekApiKey | quote }}
